<UserControl x:Class="MesTechStok.Desktop.Views.OpenCart.OpenCartSyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SyncButton" TargetType="Button">
            <Setter Property="Background" Value="#E96B20"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="8,8,8,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="OpenCart Senkronizasyon" FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="Tum OpenCart sitelerinin sync durumu" FontSize="12" Foreground="#7F8C8D"/>
                </StackPanel>
                <Button DockPanel.Dock="Right" Style="{StaticResource SyncButton}" Content="Tumunu Senkronize Et"
                        HorizontalAlignment="Right" Command="{Binding SyncAllCommand}"/>
            </DockPanel>
        </Border>

        <!-- Sync Status Grid -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="8,4,8,8">
            <DataGrid ItemsSource="{Binding SyncStatuses}" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      BorderThickness="0" Background="White" RowHeight="50" ColumnHeaderHeight="40">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F9FA"/>
                        <Setter Property="Foreground" Value="#495057"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="BorderBrush" Value="#DEE2E6"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Site" Binding="{Binding SiteName}" Width="180"/>
                    <DataGridTextColumn Header="URL" Binding="{Binding SiteUrl}" Width="200"/>
                    <DataGridTextColumn Header="Durum" Binding="{Binding Status}" Width="120"/>
                    <DataGridTextColumn Header="Urunler" Binding="{Binding SyncedProducts}" Width="90"/>
                    <DataGridTextColumn Header="Siparisler" Binding="{Binding SyncedOrders}" Width="90"/>
                    <DataGridTextColumn Header="Son Sync" Binding="{Binding LastSync, StringFormat=dd.MM.yyyy HH:mm}" Width="140"/>
                    <DataGridTextColumn Header="Hata" Binding="{Binding ErrorCount}" Width="70"/>
                    <DataGridTextColumn Header="Sonraki" Binding="{Binding NextSync, StringFormat=HH:mm}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
