<UserControl x:Class="MesTechStok.Desktop.Views.OpenCart.OpenCartSitesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SiteCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="6" ShadowDepth="1" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005a9e"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#28A745"/>
        </Style>
        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#ADB5BD"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <!-- Header -->
            <Border Style="{StaticResource CardBorder}">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                        <TextBlock Text="&#xE774;" FontFamily="Segoe MDL2 Assets" FontSize="24" VerticalAlignment="Center" Foreground="#E96B20" Margin="0,0,12,0"/>
                        <StackPanel>
                            <TextBlock Text="OpenCart Siteleri" FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
                            <TextBlock Text="OpenCart e-ticaret sitelerinizi yonetin" FontSize="12" Foreground="#7F8C8D"/>
                        </StackPanel>
                    </StackPanel>
                    <Button DockPanel.Dock="Right" Style="{StaticResource SuccessButton}" Content="+ Yeni Site Ekle"
                            HorizontalAlignment="Right" Command="{Binding AddSiteCommand}"/>
                </DockPanel>
            </Border>

            <!-- Site Kartlari -->
            <ItemsControl ItemsSource="{Binding Sites}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource SiteCard}" Width="380" BorderBrush="{Binding StatusBorderColor}">
                            <StackPanel>
                                <!-- Site Header -->
                                <DockPanel Margin="0,0,0,16">
                                    <StackPanel DockPanel.Dock="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="12" Height="12" Fill="{Binding StatusColor}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Name}" FontSize="17" FontWeight="Bold" Foreground="#2C3E50"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Url}" FontSize="12" Foreground="#6C757D" Margin="20,2,0,0"/>
                                    </StackPanel>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding StatusText}" FontSize="11"
                                               Foreground="{Binding StatusColor}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                                </DockPanel>

                                <!-- Separator -->
                                <Border Height="1" Background="#E9ECEF" Margin="0,0,0,16"/>

                                <!-- Stats -->
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                        <TextBlock Style="{StaticResource StatValue}" Text="{Binding ProductCount}" Foreground="#007ACC" HorizontalAlignment="Center"/>
                                        <TextBlock Style="{StaticResource StatLabel}" Text="Urun" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                        <TextBlock Style="{StaticResource StatValue}" Text="{Binding OrderCount}" Foreground="#F27A1A" HorizontalAlignment="Center"/>
                                        <TextBlock Style="{StaticResource StatLabel}" Text="Siparis" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                        <TextBlock Style="{StaticResource StatValue}" Text="{Binding CustomerCount}" Foreground="#28A745" HorizontalAlignment="Center"/>
                                        <TextBlock Style="{StaticResource StatLabel}" Text="Musteri" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Son Sync Zamani -->
                                <Border Background="#F8F9FA" CornerRadius="6" Padding="10,6" Margin="0,0,0,12">
                                    <DockPanel>
                                        <TextBlock DockPanel.Dock="Left" Text="Son Sync:" FontSize="12" Foreground="#6C757D"/>
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding LastSyncText}" FontSize="12" FontWeight="SemiBold"
                                                   Foreground="#495057" HorizontalAlignment="Right"/>
                                    </DockPanel>
                                </Border>

                                <!-- Butonlar -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Style="{StaticResource PrimaryButton}" Content="Detay" Margin="0,0,8,0"
                                            Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Style="{StaticResource SuccessButton}" Content="Sync"
                                            Command="{Binding DataContext.SyncSiteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
