<UserControl x:Class="MesTechStok.Desktop.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:components="clr-namespace:MesTechStok.Desktop.Components">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <!-- Modern Card Style -->
        <Style x:Key="OrderCard" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#F8F9FA" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005a9e"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#28A745"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#DC3545"/>
        </Style>

        <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#FFC107"/>
            <Setter Property="Foreground" Value="#212529"/>
        </Style>

        <Style x:Key="InfoButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#17A2B8"/>
        </Style>

        <!-- Status Badge Styles -->
        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F1F3F4"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#F5F7FA" Padding="0,0,0,10">
        <Grid Margin="15" SnapsToDevicePixels="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="💰" FontSize="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <StackPanel>
                            <TextBlock Text="Sipariş Yönetimi" 
                                      FontSize="24" 
                                      FontWeight="Bold" 
                                      Foreground="#2C3E50"/>
                            <TextBlock Text="Satış işlemleri, sipariş takibi ve müşteri siparişleri" 
                                      FontSize="13" 
                                      Foreground="#6C757D"
                                      Margin="0,3,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="🛒 Yeni Sipariş" 
                               Style="{StaticResource ModernSuccessButtonStyle}"
                                Click="AddOrder_Click"
                                Visibility="{Binding CanCreateOrders, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}"
                               Margin="5,0"/>
                        <Button Content="📤 Dışa Aktar" 
                               Style="{StaticResource ModernButtonStyle}"
                               Click="ExportOrders_Click"
                               Margin="5,0"/>
                        <Button Content="🔄 Yenile" 
                               Style="{StaticResource ModernButtonStyle}"
                               Click="RefreshOrders_Click"
                               Margin="5,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Statistics and Filters -->
            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Statistics Cards -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,5,0">
                        <StackPanel Width="130">
                            <TextBlock Text="Toplam Sipariş" FontSize="12" Foreground="#6C757D" Margin="0,0,0,5"/>
                            <TextBlock x:Name="TotalOrdersText" Text="1,247" FontSize="24" FontWeight="Bold" Foreground="#007ACC"/>
                            <TextBlock Text="+45 bugün" FontSize="10" Foreground="#28A745"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,5,0">
                        <StackPanel Width="130">
                            <TextBlock Text="Bekleyen" FontSize="12" Foreground="#6C757D" Margin="0,0,0,5"/>
                            <TextBlock x:Name="PendingOrdersText" Text="73" FontSize="24" FontWeight="Bold" Foreground="#FFC107"/>
                            <TextBlock Text="işlem bekliyor" FontSize="10" Foreground="#6C757D"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,5,0">
                        <StackPanel Width="130">
                            <TextBlock Text="Tamamlanan" FontSize="12" Foreground="#6C757D" Margin="0,0,0,5"/>
                            <TextBlock x:Name="CompletedOrdersText" Text="1,089" FontSize="24" FontWeight="Bold" Foreground="#28A745"/>
                            <TextBlock Text="87% başarı" FontSize="10" Foreground="#28A745"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Style="{StaticResource OrderCard}" Margin="0,0,5,0">
                        <StackPanel Width="130">
                            <TextBlock Text="İptal Edilen" FontSize="12" Foreground="#6C757D" Margin="0,0,0,5"/>
                            <TextBlock x:Name="CancelledOrdersText" Text="85" FontSize="24" FontWeight="Bold" Foreground="#DC3545"/>
                            <TextBlock Text="7% oran" FontSize="10" Foreground="#DC3545"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Style="{StaticResource ModernCardStyle}">
                        <StackPanel Width="130">
                            <TextBlock Text="Günlük Ciro" FontSize="12" Foreground="#6C757D" Margin="0,0,0,5"/>
                            <TextBlock x:Name="DailyRevenueText" Text="₺15,248" FontSize="24" FontWeight="Bold" Foreground="#17A2B8"/>
                            <TextBlock Text="bugün" FontSize="10" Foreground="#6C757D"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
                
                <!-- Search and Filter Panel -->
                <Border Grid.Column="2" Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <TextBlock Text="🔍 Arama ve Filtreler" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBox Grid.Row="0" x:Name="SearchTextBox" 
                                    Text="" 
                                    Style="{StaticResource ModernTextBoxStyle}"
                                    TextChanged="SearchTextBox_TextChanged"/>
                            
                            <ComboBox Grid.Row="2" x:Name="StatusFilterComboBox" 
                                     Style="{StaticResource ModernComboBoxStyle}"
                                     SelectionChanged="StatusFilter_SelectionChanged">
                                <ComboBoxItem Content="Tüm Durumlar" IsSelected="True"/>
                                <ComboBoxItem Content="Bekleyen"/>
                                <ComboBoxItem Content="Hazırlanıyor"/>
                                <ComboBoxItem Content="Kargoda"/>
                                <ComboBoxItem Content="Teslim Edildi"/>
                                <ComboBoxItem Content="İptal Edildi"/>
                            </ComboBox>
                            
                            <ComboBox Grid.Row="4" x:Name="PaymentFilterComboBox" 
                                     Style="{StaticResource ModernComboBoxStyle}">
                                <ComboBoxItem Content="Tüm Ödemeler" IsSelected="True"/>
                                <ComboBoxItem Content="Nakit"/>
                                <ComboBoxItem Content="Kredi Kartı"/>
                                <ComboBoxItem Content="Havale"/>
                                <ComboBoxItem Content="Çek"/>
                            </ComboBox>
                            
                            <ComboBox Grid.Row="6" x:Name="DateFilterComboBox" 
                                     Style="{StaticResource ModernComboBoxStyle}">
                                <ComboBoxItem Content="Tüm Tarihler" IsSelected="True"/>
                                <ComboBoxItem Content="Bugün"/>
                                <ComboBoxItem Content="Bu Hafta"/>
                                <ComboBoxItem Content="Bu Ay"/>
                                <ComboBoxItem Content="Geçen Ay"/>
                            </ComboBox>
                            
                            <Button Grid.Row="8" Content="🗑️ Filtreleri Temizle" 
                                   Style="{StaticResource WarningButton}"
                                   FontSize="11"
                                   Click="ClearFilters_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- Orders List -->
                    <Border Grid.Row="2" Style="{StaticResource ModernCardStyle}" Margin="0,10,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="📋" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Sipariş Listesi" FontSize="16" FontWeight="Bold" Foreground="#2C3E50"/>
                        <TextBlock x:Name="OrderCountText" Text="(1,247 sipariş)" FontSize="13" Foreground="#6C757D" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Loading Indicator -->
                    <Border Grid.Row="1" x:Name="LoadingBorder" 
                           Background="White" 
                           Opacity="0.8" 
                           Visibility="Collapsed"
                           Panel.ZIndex="999">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="⏳" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10">
                                <TextBlock.RenderTransform>
                                    <RotateTransform CenterX="16" CenterY="16"/>
                                </TextBlock.RenderTransform>
                                <TextBlock.Triggers>
                                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                               From="0" To="360" Duration="0:0:2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </TextBlock.Triggers>
                            </TextBlock>
                            <TextBlock Text="Siparişler yükleniyor..." FontSize="14" Foreground="#6C757D" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <DataGrid Grid.Row="1" x:Name="OrdersDataGrid" 
                             Style="{StaticResource ModernDataGridStyle}"
                              SelectionChanged="OrdersDataGrid_SelectionChanged"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              EnableRowVirtualization="True" EnableColumnVirtualization="True"
                              ColumnHeaderStyle="{StaticResource ModernColumnHeaderStyle}"
                              RowStyle="{StaticResource ModernRowStyle}">
                        <DataGrid.ContextMenu>
                            <ContextMenu Style="{StaticResource ModernContextMenuStyle}">
                                <MenuItem Header="👁️ Detaylar" Click="ViewOrder_Click" Style="{StaticResource ModernMenuItemStyle}"/>
                                <MenuItem Header="✏️ Düzenle" Click="EditOrder_Click" Style="{StaticResource ModernMenuItemStyle}"/>
                                <MenuItem Header="📋 Durum Güncelle" Click="UpdateStatus_Click" Style="{StaticResource ModernMenuItemStyle}"/>
                                <Separator/>
                                <MenuItem Header="🗑️ İptal Et" Click="CancelOrder_Click" Style="{StaticResource ModernMenuItemStyle}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Sipariş No" Binding="{Binding OrderNumber}" Width="140" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Müşteri" Binding="{Binding CustomerName}" Width="150" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Tarih" Binding="{Binding FormattedDate}" Width="130" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Tutar" Binding="{Binding FormattedAmount}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Son Güncelleme" Binding="{Binding FormattedLastUpdate}" Width="130" IsReadOnly="True"/>
                            
                            <DataGridTemplateColumn Header="Durum" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource StatusBadge}" x:Name="StatusBorder">
                                            <TextBlock x:Name="StatusText" Text="{Binding Status}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        </Border>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Bekleyen">
                                                <Setter TargetName="StatusBorder" Property="Background" Value="#FFF3CD"/>
                                                <Setter TargetName="StatusText" Property="Foreground" Value="#856404"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Hazırlanıyor">
                                                <Setter TargetName="StatusBorder" Property="Background" Value="#CCE5FF"/>
                                                <Setter TargetName="StatusText" Property="Foreground" Value="#004085"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Kargoda">
                                                <Setter TargetName="StatusBorder" Property="Background" Value="#D1ECF1"/>
                                                <Setter TargetName="StatusText" Property="Foreground" Value="#0C5460"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Teslim Edildi">
                                                <Setter TargetName="StatusBorder" Property="Background" Value="#D4EDDA"/>
                                                <Setter TargetName="StatusText" Property="Foreground" Value="#155724"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="İptal Edildi">
                                                <Setter TargetName="StatusBorder" Property="Background" Value="#F8D7DA"/>
                                                <Setter TargetName="StatusText" Property="Foreground" Value="#721C24"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTemplateColumn Header="İşlemler" Width="250">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="👁️" ToolTip="Detaylar" 
                                                   Style="{StaticResource ModernButtonStyle}"
                                                   FontSize="10" Padding="6,4" Margin="2"
                                                   Click="ViewOrder_Click"/>
                                            <Button Content="✏️" ToolTip="Düzenle" 
                                                   Style="{StaticResource ModernWarningButtonStyle}"
                                                   FontSize="10" Padding="6,4" Margin="2"
                                                    Visibility="{Binding DataContext.CanEditOrders, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}"
                                                   Click="EditOrder_Click"/>
                                            <Button Content="📋" ToolTip="Durum Güncelle" 
                                                   Style="{StaticResource ModernButtonStyle}"
                                                   FontSize="10" Padding="6,4" Margin="2"
                                                    Visibility="{Binding DataContext.CanUpdateOrderStatus, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}"
                                                   Click="UpdateStatus_Click"/>
                                            <Button Content="🗑️" ToolTip="İptal Et" 
                                                   Style="{StaticResource ModernErrorButtonStyle}"
                                                   FontSize="10" Padding="6,4" Margin="2"
                                                    Visibility="{Binding DataContext.CanCancelOrders, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}"
                                                   Click="CancelOrder_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- Enhanced Pagination Component -->
                    <components:PaginationComponent Grid.Row="2" 
                                                  x:Name="OrdersPaginationComponent"
                                                  Margin="0,20,0,0"
                                                  PageChanged="OrdersPagination_PageChanged"
                                                  PageSizeChanged="OrdersPagination_PageSizeChanged"/>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl> 