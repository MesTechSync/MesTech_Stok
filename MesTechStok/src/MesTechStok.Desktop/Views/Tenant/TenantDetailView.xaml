<UserControl x:Class="MesTechStok.Desktop.Views.Tenant.TenantDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sol: Tenant Bilgileri -->
            <StackPanel Grid.Column="0">
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Tenant Bilgileri" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,16"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="Tenant Adi *"/>
                        <TextBox Style="{StaticResource FormTextBox}" Text="{Binding TenantName, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="E-posta Adresi *"/>
                        <TextBox Style="{StaticResource FormTextBox}" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="Telefon"/>
                        <TextBox Style="{StaticResource FormTextBox}" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="Adres"/>
                        <TextBox Style="{StaticResource FormTextBox}" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                                 TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="Vergi No"/>
                        <TextBox Style="{StaticResource FormTextBox}" Text="{Binding TaxNumber, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="Notlar"/>
                        <TextBox Style="{StaticResource FormTextBox}" Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                                 TextWrapping="Wrap" AcceptsReturn="True" Height="80"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                            <Button Style="{StaticResource PrimaryButton}" Content="Kaydet" Margin="0,0,8,0"
                                    Command="{Binding SaveCommand}"/>
                            <Button Content="Iptal" Padding="16,10" FontSize="13" Cursor="Hand"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Command="{Binding CancelCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Sag: Magaza Listesi -->
            <StackPanel Grid.Column="1">
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Bagli Magazalar" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,12"/>
                        <ItemsControl ItemsSource="{Binding Stores}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E9ECEF" BorderThickness="0,0,0,1" Padding="8" Margin="0,0,0,4">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                            <TextBlock Text="{Binding Platform}" FontSize="11" Foreground="#6C757D"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="Henuz magaza eklenmemis" Foreground="#ADB5BD" FontStyle="Italic" Margin="0,8,0,0"
                                   Visibility="{Binding HasNoStores, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
