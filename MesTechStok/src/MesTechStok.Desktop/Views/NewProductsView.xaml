<UserControl x:Class="MesTechStok.Desktop.Views.NewProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid Background="#F8F9FA" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="15" Padding="20" Margin="10">
            <StackPanel>
                <TextBlock Text="📦 Ürün Yönetimi" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="Demo ürün listesi yüklendi!" FontSize="14" Foreground="#6C757D" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Content -->
        <Border Grid.Row="2" Background="White" CornerRadius="15" Padding="20" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Search -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Grid.Column="0" x:Name="SearchTextBox" 
                            Text="Ürün ara..." Padding="8" Margin="0,0,10,0"/>
                    
                    <ComboBox Grid.Column="1" x:Name="CategoryFilterComboBox">
                        <ComboBoxItem Content="Tüm Kategoriler" IsSelected="True"/>
                        <ComboBoxItem Content="Elektronik"/>
                        <ComboBoxItem Content="İçecek"/>
                        <ComboBoxItem Content="Atıştırmalık"/>
                        <ComboBoxItem Content="Kozmetik"/>
                        <ComboBoxItem Content="Spor"/>
                        <ComboBoxItem Content="Gıda"/>
                        <ComboBoxItem Content="Oyuncak"/>
                    </ComboBox>
                </Grid>
                
                <!-- DataGrid -->
                <DataGrid Grid.Row="1" x:Name="ProductsDataGrid" 
                         AutoGenerateColumns="False" 
                         Background="White"
                         GridLinesVisibility="Horizontal"
                         HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Barkod" Binding="{Binding Barcode}" Width="120"/>
                        <DataGridTextColumn Header="Ürün Adı" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Kategori" Binding="{Binding Category}" Width="100"/>
                        <DataGridTextColumn Header="Fiyat" Binding="{Binding Price, StringFormat=₺{0:F2}}" Width="80"/>
                        <DataGridTextColumn Header="Stok" Binding="{Binding Stock}" Width="60"/>
                        <DataGridTextColumn Header="Durum" Binding="{Binding Status}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
                
                <!-- Stats -->
                <Grid Grid.Row="2" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="TotalProductsText" Text="0" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Toplam Ürün" FontSize="10"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1">
                        <TextBlock x:Name="LowStockText" Text="0" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Düşük Stok" FontSize="10"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2">
                        <TextBlock x:Name="TotalValueText" Text="₺0" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Toplam Değer" FontSize="10"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="3">
                        <TextBlock x:Name="AveragePriceText" Text="₺0" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Ortalama Fiyat" FontSize="10"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="4">
                        <TextBlock x:Name="CriticalStockText" Text="0" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Kritik Stok" FontSize="10"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
        
        <!-- Hidden elements for code compatibility -->
        <StackPanel x:Name="SelectedProductPanel" Visibility="Collapsed"/>
        <StackPanel x:Name="ProductDetailsPanel" Visibility="Collapsed">
            <TextBlock x:Name="SelectedProductName"/>
            <TextBlock x:Name="SelectedProductBarcode"/>
            <TextBlock x:Name="SelectedProductCategory"/>
            <TextBlock x:Name="SelectedProductPrice"/>
            <TextBlock x:Name="SelectedProductStock"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Grid.RowSpan="3" Background="#80FFFFFF" Visibility="Collapsed" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="#2855AC"/>
                <TextBlock Text="Yukleniyor..." FontSize="13" Foreground="#6C757D" HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Empty State -->
        <Border x:Name="EmptyState" Grid.Row="2" Visibility="Collapsed" Background="White" CornerRadius="15" Margin="10" Panel.ZIndex="50">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="&#xE8D4;" FontFamily="Segoe MDL2 Assets" FontSize="48" Foreground="#D1D5DB" HorizontalAlignment="Center"/>
                <TextBlock Text="Yeni urun bulunamadi" FontSize="15" Foreground="#6C757D" HorizontalAlignment="Center" Margin="0,12,0,0"/>
                <TextBlock Text="Urun eklemek icin yukaridaki butonu kullanin" FontSize="12" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Error State -->
        <Border x:Name="ErrorState" Grid.RowSpan="3" Visibility="Collapsed" Background="#F8F9FA" Panel.ZIndex="50">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="&#xEA39;" FontFamily="Segoe MDL2 Assets" FontSize="48" Foreground="#DC3545" HorizontalAlignment="Center"/>
                <TextBlock x:Name="ErrorMessage" Text="Bir hata olustu" FontSize="15" Foreground="#6C757D" HorizontalAlignment="Center" Margin="0,12,0,0"/>
                <Button Content="Tekrar Dene" Click="RetryButton_Click" Margin="0,12,0,0" Padding="16,8" Background="#2855AC" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl> 