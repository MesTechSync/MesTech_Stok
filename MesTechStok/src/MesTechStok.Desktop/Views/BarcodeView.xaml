<UserControl x:Class="MesTechStok.Desktop.Views.BarcodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="BarcodeView_Loaded"
             Unloaded="BarcodeView_Unloaded"
             SizeChanged="BarcodeView_SizeChanged">
    
    <UserControl.Resources>
        <!-- Modern Card Style -->
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#F8F9FA" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="3" Opacity="0.12"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Camera Frame Style -->
        <Style x:Key="CameraFrame" TargetType="Border">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="BorderBrush" Value="#007ACC"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#007ACC" BlurRadius="10" ShadowDepth="0" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005a9e"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004578"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#6C757D"/>
        </Style>
        
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#28A745"/>
        </Style>
        
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#DC3545"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#F8F9FA">
        <Grid Margin="20">
            <!-- Header kaldırıldı -->

            <!-- Main Content -->
            <Grid x:Name="MainGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="LeftColumn" Width="2*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition x:Name="RightColumn" Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Camera Section -->
                <Border Grid.Column="0" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <!-- Camera Header -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                            <TextBlock Text="📹" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Kamera Görüntüsü" FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
                        </StackPanel>
                        
                        <!-- Small Status (preview kapalıyken bile görünsün) -->
                        <Border Background="#EEF6FF" BorderBrush="#CCE4FF" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock x:Name="CameraStatusText"
                                           Text="🎥 Kamera Kapalı"
                                           FontSize="14"
                                           Foreground="#6C757D"/>
                                <TextBlock x:Name="ScanStatusText"
                                           Text="Barkod taramaya hazır değil"
                                           FontSize="12"
                                           Foreground="#6C757D"/>
                            </StackPanel>
                        </Border>

                        <!-- Camera Display (varsayılan gizli) -->
                        <Border x:Name="CameraHost" Style="{StaticResource CameraFrame}" Margin="0,0,0,20" MinHeight="360" ClipToBounds="True" SizeChanged="CameraHost_SizeChanged">
                            <Grid>
                                <!-- Camera Background -->
                                <Rectangle Fill="#1A1A1A"/>

                                <!-- Camera Preview Image (UniformToFill + overscan render transform) -->
                                <Image x:Name="CameraPreview"
                                       Stretch="UniformToFill"
                                       RenderOptions.BitmapScalingMode="HighQuality"
                                       SnapsToDevicePixels="True"
                                       UseLayoutRounding="True"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Stretch"
                                       RenderTransformOrigin="0.5,0.5">
                                    <Image.RenderTransform>
                                        <TransformGroup>
                                            <ScaleTransform x:Name="CameraScale" />
                                            <TranslateTransform x:Name="CameraTranslate" />
                                        </TransformGroup>
                                    </Image.RenderTransform>
                                </Image>

                                <!-- Scanning Overlay -->
                                <Canvas x:Name="ScanOverlay" IsHitTestVisible="False">
                                    <!-- Scanning Line Animation -->
                                    <Rectangle x:Name="ScanLine" 
                                              Width="{Binding ElementName=CameraHost, Path=ActualWidth}"
                                              Height="2" 
                                              Fill="#00FF00" 
                                              Canvas.Left="0"
                                              Opacity="0"/>
                                    
                                    <!-- Corner Brackets -->
                                    <Rectangle Width="50" Height="3" Fill="#007ACC" Canvas.Left="100" Canvas.Top="120"/>
                                    <Rectangle Width="3" Height="50" Fill="#007ACC" Canvas.Left="100" Canvas.Top="120"/>
                                    <Rectangle Width="50" Height="3" Fill="#007ACC" Canvas.Right="100" Canvas.Top="120"/>
                                    <Rectangle Width="3" Height="50" Fill="#007ACC" Canvas.Right="100" Canvas.Top="120"/>
                                    <Rectangle Width="50" Height="3" Fill="#007ACC" Canvas.Left="100" Canvas.Bottom="120"/>
                                    <Rectangle Width="3" Height="50" Fill="#007ACC" Canvas.Left="100" Canvas.Bottom="120"/>
                                    <Rectangle Width="50" Height="3" Fill="#007ACC" Canvas.Right="100" Canvas.Bottom="120"/>
                                    <Rectangle Width="3" Height="50" Fill="#007ACC" Canvas.Right="100" Canvas.Bottom="120"/>
                                </Canvas>
                                
                            </Grid>
                        </Border>
                        
                        <!-- Camera Controls -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Button Grid.Column="0" 
                                   x:Name="StartCameraBtn"
                                   Content="🎥 Kamerayı Başlat" 
                                   Style="{StaticResource SuccessButton}"
                                   Click="StartCamera_Click"/>
                            
                            <Button Grid.Column="2" 
                                   x:Name="ScanBarcodeBtn"
                                   Content="📱 Barkod Tara" 
                                   Style="{StaticResource PrimaryButton}"
                                   Click="ScanBarcode_Click"
                                   IsEnabled="False"/>
                            
                            <Button Grid.Column="4" 
                                   x:Name="StopCameraBtn"
                                   Content="⏹️ Durdur" 
                                   Style="{StaticResource DangerButton}"
                                   Click="StopCamera_Click"
                                   IsEnabled="False"/>
                        </Grid>

                        <!-- Global Barkod Test Butonu -->
                        <Grid Margin="0,15,0,0">
                            <Button x:Name="TestGlobalBarcodeBtn"
                                   Content="🧪 Global Barkod Test (Test123)" 
                                   Style="{StaticResource PrimaryButton}"
                                   Click="TestGlobalBarcode_Click"
                                   ToolTip="Global barkod servisini test etmek için simüle edilmiş barkod gönderir"/>
                        </Grid>

                        <!-- Overscan / Önizleme Ayarları -->
                        <Expander Header="Görüntü Ayarları" Margin="0,12,0,0" IsExpanded="False">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,6">
                                    <CheckBox x:Name="PreviewToggle" Content="Kamera Önizlemesini Göster" IsChecked="True" Checked="PreviewToggle_Checked" Unchecked="PreviewToggle_Unchecked"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <CheckBox x:Name="OverscanToggle" IsChecked="True" Margin="0,0,10,0" Checked="OverscanToggle_Checked" Unchecked="OverscanToggle_Unchecked"/>
                                    <TextBlock Text="Overscan Aktif" VerticalAlignment="Center"/>
                                </StackPanel>
                                <Grid Margin="0,10,0,0" IsEnabled="{Binding ElementName=PreviewToggle, Path=IsChecked}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="Genişlik Katsayısı"/>
                                        <Slider x:Name="WidthMulSlider" Minimum="1.0" Maximum="3.0" Value="2.5" TickFrequency="0.05" IsSnapToTickEnabled="True" ValueChanged="WidthMulSlider_ValueChanged"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" x:Name="WidthMulValueText" Text="2.50x" VerticalAlignment="Center"/>
                                </Grid>
                                <Grid Margin="0,6,0,0" IsEnabled="{Binding ElementName=PreviewToggle, Path=IsChecked}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="Yükseklik Katsayısı"/>
                                        <Slider x:Name="HeightMulSlider" Minimum="1.0" Maximum="2.0" Value="1.333" TickFrequency="0.05" IsSnapToTickEnabled="True" ValueChanged="HeightMulSlider_ValueChanged"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" x:Name="HeightMulValueText" Text="1.33x" VerticalAlignment="Center"/>
                                </Grid>
                            </StackPanel>
                        </Expander>

                        <!-- Okuyucu/Format Ayarları -->
                        <Expander Header="Okuyucu / Format Ayarları" Margin="0,12,0,0" IsExpanded="False">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <CheckBox x:Name="UpceToggle"
                                              Content="UPC-E'yi etkinleştir"
                                              IsChecked="False"
                                              Checked="UpceToggle_Checked"
                                              Unchecked="UpceToggle_Unchecked"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <CheckBox x:Name="GlossyToggle"
                                              Content="Parlak Yüzey Optimizasyonu"
                                              IsChecked="False"
                                              Checked="GlossyToggle_Checked"
                                              Unchecked="GlossyToggle_Unchecked"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <CheckBox x:Name="MatteToggle"
                                              Content="Mat Yüzey Optimizasyonu"
                                              IsChecked="False"
                                              Checked="MatteToggle_Checked"
                                              Unchecked="MatteToggle_Unchecked"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <CheckBox x:Name="NearToggle"
                                              Content="Yakın Mesafe Optimizasyonu"
                                              IsChecked="False"
                                              Checked="NearToggle_Checked"
                                              Unchecked="NearToggle_Unchecked"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="Tek Format (Seçersen sadece bunu dener)"/>
                                    <ComboBox x:Name="SingleFormatCombo"
                                              SelectionChanged="SingleFormatCombo_SelectionChanged"
                                              Width="260">
                                        <ComboBoxItem Content="Auto" IsSelected="True"/>
                                        <ComboBoxItem Content="EAN_13"/>
                                        <ComboBoxItem Content="EAN_8"/>
                                        <ComboBoxItem Content="UPC_A"/>
                                        <ComboBoxItem Content="UPC_E"/>
                                        <ComboBoxItem Content="CODE_128"/>
                                        <ComboBoxItem Content="CODE_39"/>
                                        <ComboBoxItem Content="ITF"/>
                                        <ComboBoxItem Content="QR_CODE"/>
                                        <ComboBoxItem Content="DATA_MATRIX"/>
                                    </ComboBox>
                                </StackPanel>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </Border>
                
                <!-- Results Panel -->
                <Border Grid.Column="2" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <!-- Results Header -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                            <TextBlock Text="📊" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Tarama Sonuçları" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                        </StackPanel>
                        
                        <!-- Current Scan -->
                        <Border Background="#E8F5E8" BorderBrush="#28A745" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="📱 Son Taranan Barkod" FontSize="12" Foreground="#155724" FontWeight="Bold"/>
                                <TextBlock x:Name="LastBarcodeText" Text="Henüz tarama yapılmadı" FontSize="14" Foreground="#155724" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Product Info -->
                        <Border x:Name="ProductInfoPanel" Background="#FFF3CD" BorderBrush="#FFC107" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="📦 Ürün Bilgisi" FontSize="12" Foreground="#856404" FontWeight="Bold"/>
                                <TextBlock x:Name="ProductNameText" Text="" FontSize="14" Foreground="#856404" Margin="0,5,0,0"/>
                                <TextBlock x:Name="ProductPriceText" Text="" FontSize="12" Foreground="#856404" Margin="0,3,0,0"/>
                                <TextBlock x:Name="ProductStockText" Text="" FontSize="12" Foreground="#856404" Margin="0,3,0,0"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Error Panel -->
                        <Border x:Name="ErrorPanel" Background="#F8D7DA" BorderBrush="#DC3545" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="❌ Hata" FontSize="12" Foreground="#721C24" FontWeight="Bold"/>
                                <TextBlock x:Name="ErrorText" Text="" FontSize="14" Foreground="#721C24" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Scan History -->
                        <StackPanel>
                            <TextBlock Text="📋 Tarama Geçmişi" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                            <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="ScanHistoryPanel">
                                    <TextBlock Text="Henüz tarama geçmişi yok" FontSize="12" Foreground="#6C757D" Style="{x:Null}"/>
                                </StackPanel>
                            </ScrollViewer>
                        </StackPanel>
                        
                        <!-- Quick Actions -->
                        <StackPanel Margin="0,20,0,0">
                            <TextBlock Text="⚡ Hızlı İşlemler" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                            
                            <Button Content="🔄 Geçmişi Temizle" 
                                   Style="{StaticResource SecondaryButton}"
                                   Click="ClearHistory_Click"
                                   Margin="0,5"/>
                            
                            <Button Content="📊 Test Barkodu Tara" 
                                   Style="{StaticResource PrimaryButton}"
                                   Click="TestScan_Click"
                                   Margin="0,5"/>
                            
                            <Button Content="📱 Manuel Barkod Gir" 
                                   Style="{StaticResource SecondaryButton}"
                                   Click="ManualBarcode_Click"
                                   Margin="0,5"/>
                        </StackPanel>

                        <!-- Barkod Log Geçmişi (DB) kaldırıldı: Artık tüm log akışı LogView üzerinden yönetiliyor -->
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl> 