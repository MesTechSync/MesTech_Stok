<UserControl x:Class="MesTechStok.Desktop.Views.Store.StoreDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TestButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#F27A1A"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sol: Magaza Bilgileri -->
            <StackPanel Grid.Column="0">
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Magaza Bilgileri" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,16"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="Magaza Adi *"/>
                        <TextBox Style="{StaticResource FormTextBox}" Text="{Binding StoreName, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="Tenant (Magaza Sahibi) *"/>
                        <ComboBox Padding="10,8" FontSize="13" Margin="0,0,0,12"
                                  ItemsSource="{Binding Tenants}" SelectedItem="{Binding SelectedTenant}"
                                  DisplayMemberPath="Name"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="Platform *"/>
                        <ComboBox Padding="10,8" FontSize="13" Margin="0,0,0,12"
                                  ItemsSource="{Binding Platforms}" SelectedItem="{Binding SelectedPlatform}"
                                  DisplayMemberPath="Name"/>

                        <TextBlock Style="{StaticResource FormLabel}" Text="Aciklama"/>
                        <TextBox Style="{StaticResource FormTextBox}" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                 TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Sag: Platform Credential Alanlari (Dinamik) -->
            <StackPanel Grid.Column="1">
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Platform API Bilgileri" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,16"/>
                        <TextBlock Text="{Binding CredentialHelpText}" FontSize="11" Foreground="#6C757D" TextWrapping="Wrap" Margin="0,0,0,12"/>

                        <!-- Trendyol Credentials -->
                        <StackPanel Visibility="{Binding IsTrendyol, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Style="{StaticResource FormLabel}" Text="API Key *"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Style="{StaticResource FormLabel}" Text="API Secret *"/>
                            <PasswordBox Padding="10,8" FontSize="13" Margin="0,0,0,12" BorderBrush="#CED4DA" BorderThickness="1"/>
                            <TextBlock Style="{StaticResource FormLabel}" Text="Supplier ID *"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding SupplierId, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <!-- N11 Credentials -->
                        <StackPanel Visibility="{Binding IsN11, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Style="{StaticResource FormLabel}" Text="App Key *"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding AppKey, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Style="{StaticResource FormLabel}" Text="App Secret *"/>
                            <PasswordBox Padding="10,8" FontSize="13" Margin="0,0,0,12" BorderBrush="#CED4DA" BorderThickness="1"/>
                        </StackPanel>

                        <!-- Hepsiburada Credentials -->
                        <StackPanel Visibility="{Binding IsHepsiburada, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Style="{StaticResource FormLabel}" Text="Username *"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding HbUsername, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Style="{StaticResource FormLabel}" Text="Password *"/>
                            <PasswordBox Padding="10,8" FontSize="13" Margin="0,0,0,12" BorderBrush="#CED4DA" BorderThickness="1"/>
                            <TextBlock Style="{StaticResource FormLabel}" Text="Merchant ID *"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding MerchantId, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <!-- Amazon Credentials -->
                        <StackPanel Visibility="{Binding IsAmazon, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Style="{StaticResource FormLabel}" Text="Client ID *"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding ClientId, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Style="{StaticResource FormLabel}" Text="Client Secret *"/>
                            <PasswordBox Padding="10,8" FontSize="13" Margin="0,0,0,12" BorderBrush="#CED4DA" BorderThickness="1"/>
                            <TextBlock Style="{StaticResource FormLabel}" Text="Refresh Token *"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding RefreshToken, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Style="{StaticResource FormLabel}" Text="Marketplace ID *"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding MarketplaceId, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <!-- Baglanti Testi -->
                        <Border Background="#F8F9FA" CornerRadius="8" Padding="16" Margin="0,8,0,0">
                            <StackPanel>
                                <Button Style="{StaticResource TestButton}" Content="Baglantiyi Test Et" HorizontalAlignment="Stretch"
                                        Command="{Binding TestConnectionCommand}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0" HorizontalAlignment="Center"
                                            Visibility="{Binding ShowTestResult, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Ellipse Width="12" Height="12" Fill="{Binding TestResultColor}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding TestResultMessage}" FontSize="13" FontWeight="SemiBold"
                                               Foreground="{Binding TestResultColor}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                            <Button Style="{StaticResource PrimaryButton}" Content="Kaydet" Margin="0,0,8,0"
                                    Command="{Binding SaveCommand}"/>
                            <Button Content="Iptal" Padding="16,10" FontSize="13" Cursor="Hand"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Command="{Binding CancelCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
