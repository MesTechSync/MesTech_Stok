<Window x:Class="MesTechStok.Desktop.Views.BarcodeIntegrationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Barkod Entegrasyonu" Height="250" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="📱 Barkod ile Ürün Ekleme" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="1" 
                   Text="Barkod okuyucu kullanarak ürün eklemek için:&#x0a;1. Kamerayı başlatın&#x0a;2. Barkodu tarayın&#x0a;3. Ürün bilgilerini tamamlayın" 
                   Margin="0,0,0,15"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Barkod:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="BarcodeTextBox" Style="{StaticResource ModernTextBox}" Text="1234567890123" Width="150"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="UseButton" Content="✅ Bu Barkodu Kullan" Style="{StaticResource ModernButton}" Width="130" Height="30" Margin="0,0,10,0" Click="UseButton_Click"/>
            <Button x:Name="CancelButton" Content="❌ İptal" Style="{StaticResource ModernButton}" Background="#6C757D" Width="80" Height="30" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 