<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:MesTech.Avalonia.ViewModels"
             x:Class="MesTech.Avalonia.Views.PipelineAvaloniaView"
             x:DataType="vm:PipelineAvaloniaViewModel">

    <Grid RowDefinitions="Auto,*" Background="{DynamicResource MesBgPage}">

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource MesPrimaryBlue}" Padding="20,14">
            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                    <TextBlock Text="Pipeline" Foreground="White"
                               FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <Border Background="#FFFFFF22" CornerRadius="10" Padding="8,2">
                        <TextBlock Text="{Binding TotalCount, StringFormat='{}{0} firsat'}"
                                   Foreground="White" FontSize="12" VerticalAlignment="Center" />
                    </Border>
                    <Border Background="#FFFFFF22" CornerRadius="10" Padding="8,2">
                        <TextBlock Text="{Binding TotalValue}" Foreground="White"
                                   FontSize="12" VerticalAlignment="Center" />
                    </Border>
                </StackPanel>
                <Button Grid.Column="1" Content="Yenile" ToolTip.Tip="Verileri yenile" Command="{Binding RefreshCommand}"
                        Background="White" Foreground="{DynamicResource MesPrimaryBlue}" FontWeight="SemiBold"
                        Padding="14,8" CornerRadius="6" />
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="22" Spacing="16">

                <!-- Loading -->
                <TextBlock Text="Yukleniyor..." IsVisible="{Binding IsLoading}"
                           Foreground="{DynamicResource MesSlateGray}" HorizontalAlignment="Center" Margin="0,20" />

                <!-- Error -->
                <Border IsVisible="{Binding HasError}" Background="#FEF2F2"
                        CornerRadius="8" Padding="16" BorderBrush="#FECACA" BorderThickness="1">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626" TextWrapping="Wrap" />
                </Border>

                <!-- Empty -->
                <TextBlock Text="Pipeline verisi bulunmuyor."
                           IsVisible="{Binding IsEmpty}" Foreground="{DynamicResource MesMutedGray}"
                           HorizontalAlignment="Center" Margin="0,40" FontSize="14" />

                <!-- Pipeline Stages as horizontal bars -->
                <ItemsControl ItemsSource="{Binding Stages}" IsVisible="{Binding !IsLoading}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:PipelineStageVm">
                            <Border Margin="0,0,0,12" Padding="20" Background="White"
                                    CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1">
                                <Grid ColumnDefinitions="Auto,*,Auto,Auto">
                                    <!-- Color indicator -->
                                    <Border Grid.Column="0" Width="6" CornerRadius="3" Margin="0,0,16,0">
                                        <Border.Background>
                                            <SolidColorBrush Color="{Binding Color}" />
                                        </Border.Background>
                                    </Border>
                                    <!-- Stage info -->
                                    <StackPanel Grid.Column="1" Spacing="4">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="15" />
                                        <TextBlock Text="{Binding DealCount, StringFormat='{}{0} firsat'}"
                                                   FontSize="12" Foreground="{DynamicResource MesSlateGray}" />
                                    </StackPanel>
                                    <!-- Amount -->
                                    <TextBlock Grid.Column="2" Text="{Binding Amount}"
                                               FontWeight="Bold" FontSize="16" Foreground="{DynamicResource MesPrimaryBlue}"
                                               VerticalAlignment="Center" Margin="0,0,20,0" />
                                    <!-- Percentage -->
                                    <Border Grid.Column="3" CornerRadius="4" Padding="10,4"
                                            VerticalAlignment="Center">
                                        <Border.Background>
                                            <SolidColorBrush Color="{Binding Color}" Opacity="0.15" />
                                        </Border.Background>
                                        <TextBlock Text="{Binding Percentage, StringFormat='%{0}'}" FontWeight="Bold" FontSize="13">
                                            <TextBlock.Foreground>
                                                <SolidColorBrush Color="{Binding Color}" />
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
