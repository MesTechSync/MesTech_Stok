<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:MesTech.Avalonia.ViewModels"
             x:Class="MesTech.Avalonia.Views.StoreWizardAvaloniaView"
             x:DataType="vm:StoreWizardAvaloniaViewModel">

    <!-- Magaza Ekleme Sihirbazi — 3 adim: Platform Sec → Kimlik → Test + Kaydet -->

    <Grid RowDefinitions="Auto,Auto,*" Background="{DynamicResource MesBgPage}">

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource MesPrimaryBlue}" Padding="20,14">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock Text="Yeni Mağaza Ekle" Foreground="{DynamicResource MesBgPrimary}"
                           FontSize="{StaticResource FontSizeH3}" FontWeight="SemiBold" VerticalAlignment="Center" />
                <Border Background="{DynamicResource MesBadgeOverlay}" CornerRadius="{StaticResource MesRadiusMdLg}" Padding="8,2">
                    <TextBlock Text="{Binding CurrentStep, StringFormat='Adım {0}/3'}"
                               Foreground="{DynamicResource MesBgPrimary}" FontSize="{StaticResource FontSizeSmall}" VerticalAlignment="Center" />
                </Border>
            </StackPanel>
        </Border>

        <!-- Step Indicator -->
        <Border Grid.Row="1" Background="{DynamicResource MesBgPrimary}" Padding="20,12"
                BorderBrush="{DynamicResource MesBorderMedium}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="24" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Border Width="28" Height="28" CornerRadius="{StaticResource MesRadiusLgXl}" Background="{DynamicResource MesPrimaryBlue}">
                        <TextBlock Text="1" Foreground="{DynamicResource MesBgPrimary}" FontSize="{StaticResource FontSizeCallout}" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="Platform Seç" VerticalAlignment="Center" FontSize="{StaticResource FontSizeCallout}" />
                </StackPanel>
                <TextBlock Text="—" Foreground="{DynamicResource MesBorderMuted}" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Border Width="28" Height="28" CornerRadius="{StaticResource MesRadiusLgXl}" Background="{DynamicResource MesMutedGray}">
                        <TextBlock Text="2" Foreground="{DynamicResource MesBgPrimary}" FontSize="{StaticResource FontSizeCallout}" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="Kimlik Bilgileri" VerticalAlignment="Center" FontSize="{StaticResource FontSizeCallout}" />
                </StackPanel>
                <TextBlock Text="—" Foreground="{DynamicResource MesBorderMuted}" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Border Width="28" Height="28" CornerRadius="{StaticResource MesRadiusLgXl}" Background="{DynamicResource MesMutedGray}">
                        <TextBlock Text="3" Foreground="{DynamicResource MesBgPrimary}" FontSize="{StaticResource FontSizeCallout}" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="Test ve Kaydet" VerticalAlignment="Center" FontSize="{StaticResource FontSizeCallout}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Step Content -->
        <ScrollViewer Grid.Row="2" Padding="24">
          <Panel>

            <!-- STEP 1: Platform Select -->
            <StackPanel IsVisible="{Binding IsStep1Visible}">
                <TextBlock Text="Platform Seçin" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Margin="0,0,0,16"
                           Foreground="{DynamicResource MesDarkSlate}" />
                <ItemsControl ItemsSource="{Binding AvailablePlatforms}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:WizardPlatformDto">
                            <Button Margin="6" Padding="0" Background="{DynamicResource MesBgPrimary}" CornerRadius="{StaticResource MesRadiusMd}"
                                    BorderBrush="{DynamicResource MesBorderMedium}" BorderThickness="1"
                                    Command="{Binding $parent[ItemsControl].((vm:StoreWizardAvaloniaViewModel)DataContext).SelectPlatformCommand}"
                                    CommandParameter="{Binding}">
                                <StackPanel Width="160">
                                    <Border Height="6" CornerRadius="8,8,0,0" Background="{Binding Color}" />
                                    <Border Padding="16,12">
                                        <StackPanel Spacing="6">
                                            <TextBlock Text="{Binding Name}" FontSize="{StaticResource FontSizeBodyLg}" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource MesDarkSlate}" />
                                            <TextBlock Text="Seç" FontSize="{StaticResource FontSizeSmall}" Foreground="{DynamicResource MesPrimaryBlue}" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- STEP 2: Credential Form -->
            <StackPanel IsVisible="{Binding IsStep2Visible}" MaxWidth="500" Spacing="16">
                <TextBlock Text="{Binding SelectedPlatform, StringFormat='{}{0} — Kimlik Bilgileri'}"
                           FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource MesDarkSlate}" />

                <StackPanel Spacing="4">
                    <TextBlock Text="Mağaza Adı" FontSize="{StaticResource FontSizeSmall}" Foreground="{DynamicResource MesSlateGray}" />
                    <TextBox Text="{Binding StoreName}" Watermark="Örn: Ana Mağaza" AutomationProperties.Name="Örn: Ana Mağaza" Padding="10,8" />
                </StackPanel>

                <StackPanel Spacing="4">
                    <TextBlock Text="{Binding Field1Label}" FontSize="{StaticResource FontSizeSmall}" Foreground="{DynamicResource MesSlateGray}" />
                    <TextBox Text="{Binding Field1Value}" Watermark="{Binding Field1Label}" AutomationProperties.Name="{Binding Field1Label}" Padding="10,8" />
                </StackPanel>

                <StackPanel Spacing="4">
                    <TextBlock Text="{Binding Field2Label}" FontSize="{StaticResource FontSizeSmall}" Foreground="{DynamicResource MesSlateGray}" />
                    <TextBox Text="{Binding Field2Value}" Watermark="{Binding Field2Label}" AutomationProperties.Name="{Binding Field2Label}"
                             PasswordChar="*" Padding="10,8" />
                </StackPanel>

                <StackPanel Spacing="4" IsVisible="{Binding Field3Visible}">
                    <TextBlock Text="{Binding Field3Label}" FontSize="{StaticResource FontSizeSmall}" Foreground="{DynamicResource MesSlateGray}" />
                    <TextBox Text="{Binding Field3Value}" Watermark="{Binding Field3Label}" AutomationProperties.Name="{Binding Field3Label}" Padding="10,8" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,16,0,0">
                    <Button Content="Geri" Command="{Binding GoBackCommand}"
                            Background="{DynamicResource MesMutedGray}" Foreground="{DynamicResource MesBgPrimary}" Padding="16,8" CornerRadius="{StaticResource MesRadiusBase}" />
                    <Button Content="İleri" Command="{Binding GoNextCommand}"
                            Background="{DynamicResource MesPrimaryBlue}" Foreground="{DynamicResource MesBgPrimary}" FontWeight="SemiBold"
                            Padding="16,8" CornerRadius="{StaticResource MesRadiusBase}" />
                </StackPanel>
            </StackPanel>

            <!-- STEP 3: Test + Save -->
            <StackPanel IsVisible="{Binding IsStep3Visible}" MaxWidth="500" Spacing="16">
                <TextBlock Text="Bağlantı Testi ve Kayıt" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold"
                           Foreground="{DynamicResource MesDarkSlate}" />

                <Border Background="{DynamicResource MesBgPrimary}" CornerRadius="{StaticResource MesRadiusMd}" Padding="20" BorderBrush="{DynamicResource MesBorderMedium}" BorderThickness="1">
                    <StackPanel Spacing="12">
                        <TextBlock Text="{Binding SelectedPlatform, StringFormat='Platform: {0}'}" FontSize="{StaticResource FontSizeBody}" />
                        <TextBlock Text="{Binding StoreName, StringFormat='Mağaza: {0}'}" FontSize="{StaticResource FontSizeBody}" />

                        <Button Content="Bağlantı Test" Command="{Binding TestConnectionCommand}"
                                Background="{DynamicResource MesPrimaryBlue}" Foreground="{DynamicResource MesBgPrimary}" FontWeight="SemiBold"
                                Padding="16,8" CornerRadius="{StaticResource MesRadiusBase}"
                                IsEnabled="{Binding !IsTesting}" />

                        <ProgressBar IsIndeterminate="True" IsVisible="{Binding IsTesting}" Height="4" />

                        <Border Background="{DynamicResource MesBgSurface}" CornerRadius="{StaticResource MesRadiusBase}" Padding="12"
                                IsVisible="{Binding TestPassed}">
                            <TextBlock Text="{Binding TestResultMessage}" Foreground="{DynamicResource MesGreen}" FontWeight="SemiBold" />
                        </Border>

                        <Border Background="{DynamicResource MesBgDangerLight}" CornerRadius="{StaticResource MesRadiusBase}" Padding="12"
                                IsVisible="{Binding TestFailed}">
                            <TextBlock Text="{Binding TestResultMessage}" Foreground="{DynamicResource MesRedAlert}" />
                        </Border>
                    </StackPanel>
                </Border>

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button Content="Geri" Command="{Binding GoBackCommand}"
                            Background="{DynamicResource MesMutedGray}" Foreground="{DynamicResource MesBgPrimary}" Padding="16,8" CornerRadius="{StaticResource MesRadiusBase}" />
                    <Button Content="Kaydet" ToolTip.Tip="Degisiklikleri kaydet" Command="{Binding SaveStoreCommand}"
                            Background="{DynamicResource MesGreenMaterial}" Foreground="{DynamicResource MesBgPrimary}" FontWeight="SemiBold"
                            Padding="16,8" CornerRadius="{StaticResource MesRadiusBase}"
                            IsEnabled="{Binding TestPassed}" />
                </StackPanel>

                <Border Background="{DynamicResource MesBgSurface}" CornerRadius="{StaticResource MesRadiusMd}" Padding="16"
                        IsVisible="{Binding SaveCompleted}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Mağaza başarıyla kaydedildi!" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold"
                                   Foreground="{DynamicResource MesGreen}" />
                        <Button Content="Yeni Mağaza Ekle" Command="{Binding ResetCommand}"
                                Background="{DynamicResource MesPrimaryBlue}" Foreground="{DynamicResource MesBgPrimary}" Padding="16,8" CornerRadius="{StaticResource MesRadiusBase}" />
                    </StackPanel>
                </Border>
            </StackPanel>
          </Panel>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3"
                Background="{DynamicResource MesOverlayLight}"
                IsVisible="{Binding IsLoading}"
                ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="240" Height="4" Margin="0,0,0,12" />
                <TextBlock Text="İşleniyor..." FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource MesTextMuted}" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
