@page "/"
@attribute [Authorize]
@inject NavigationManager Navigation

<PageTitle>MesTech - Ana Sayfa</PageTitle>

@code {
  protected override void OnInitialized()
  {
    // Redirect root to dashboard (only reached if authenticated)
    Navigation.NavigateTo("/dashboard", replace: true);
  }
}
