@page "/hr/calisanlar"
@attribute [Authorize]
@inject MesTechApiClient ApiClient

<DemoFallbackBanner IsDemoMode="@_isDemoMode" />

@if (isLoading)
{
    <LoadingSpinner />
}
else if (errorMessage is not null)
{
    <div class="alert alert-warning" role="alert">
        <i class="fas fa-exclamation-triangle me-2"></i>@errorMessage
    </div>
}
else
{
    <div class="d-flex align-items-center mb-4">
      <div class="rounded-circle d-flex align-items-center justify-content-center me-3"
           style="width: 48px; height: 48px; background-color: #2855AC;">
        <i class="fas fa-users text-white"></i>
      </div>
      <div>
        <h3 class="mb-0" style="color: #2855AC;">Calisanlar</h3>
        <small class="text-muted">Personel kayitlari ve organizasyon</small>
      </div>
    </div>

    <div class="card shadow-sm">
      <div class="card-body text-center py-5">
        <i class="fas fa-users fa-3x mb-3 d-block" style="color: #2855AC; opacity: 0.3;"></i>
        <h5 class="text-muted">Calisan Yonetimi</h5>
        <p class="text-muted mb-0">Personel kayitlari, departman yapisi, gorev tanimlari ve ozluk bilgileri.</p>
      </div>
    </div>

    <div class="alert alert-secondary mt-4" role="alert">
      <i class="fas fa-clock me-2"></i>
      <strong>Dalga 11:</strong> Bu sayfa Dalga 11'de tamamlanacak. Calisan CRUD, departman agaci,
      yetki matrisi ve performans takibi eklenecek.
    </div>
}

@code {
    private bool isLoading = true;
    private bool _isDemoMode;
    private string? errorMessage;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            var result = await ApiClient.SafeGetAsync<List<object>>("hr/employees");
            if (result.IsSuccess) { /* TODO: map result.Data */ }
            else { _isDemoMode = true; }
        }
        catch (Exception ex)
        {
            errorMessage = $"Veri yuklenirken hata: {ex.Message}";
        }
        finally
        {
            isLoading = false;
        }
    }
}
